def mergeK_Linked_lsit(k):
    # put all heads into arr
    # loop through the arr of heads, find smallest head and asgin it to be a new var
    # make newHead point to the smallest head node (SH)
    # make head to point to the node after SH (new_node)
    # and use the same loop and compare algrithm on the new_node, h1,h2
    # continue till one of the new node's next node is tail, then point head node of tht linked list to none
    # put the rest of the remaining nodes to the new linked list
